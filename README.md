# DevOps Challenge for SimplyDelivery

*I chose a s3 bucket as the origin, because I had no other origin to test.*
*Slight adjustments need to be made for general purpose https/ip-forwarding.*

## Install & start

In order to start:
```bash
$ cp terraform.example.tfvars terraform.tfvars
```
and adjust values accordingly.

You also need to

```bash
AWS_ACCESS_KEY_ID=XXXXX
AWS_SECRET_ACCESS_KEY=Cdfkjdkfjkj+djfkdjfkfjdkf
AWS_DEFAULT_REGION=us-east-2
```
